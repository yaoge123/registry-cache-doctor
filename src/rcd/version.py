"""Single source of truth for the package version.

Kept as plain assignment so it can be parsed by tooling without
importing the package (e.g. release workflows reading the version
out of source).
"""

from __future__ import annotations

__version__ = "0.1.0"
