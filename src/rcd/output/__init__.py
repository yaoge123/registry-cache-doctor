"""Output subpackage.

Concrete implementations land in Phase 3:

* :mod:`rcd.output.ndjson`     — stdout NDJSON event stream.
* :mod:`rcd.output.progress`   — stderr progress bars (TTY) / log lines (non-TTY).
* :mod:`rcd.output.stderr_log` — human-readable stderr summaries.
"""
